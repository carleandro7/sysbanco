<%= form_with(model: saque, local: true) do |form| %>
  <% if saque.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saque.errors.count, "error") %> prohibited this saque from being saved:</h2>

      <ul>
        <% saque.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :valor %>
    <%= form.text_field :valor %>
  </div>

  <div class="field">
    <%= form.label :account_id %>
    <%= form.text_field :account_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
